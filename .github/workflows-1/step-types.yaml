name: Step Types

on:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"

  say-hello-python:
    runs-on: ubuntu-latest
    steps:
      - run: print("hello world")
        shell: python
